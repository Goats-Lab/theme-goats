{% comment %}
  Section: Página de Encomendas MyCharms
{% endcomment %}

<section class="mc-encomenda">
  <style>
    .mc-encomenda {
      background: #0b0c12;
      color: #f5f5f7;
      font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", system-ui, sans-serif;
    }

    .mc-encomenda__wrap {
      max-width: 1200px;
      margin: 0 auto;
      padding: 60px 20px 90px;
    }

    /* HERO */

    .mc-encomenda__hero {
      border-radius: 22px;
      padding: 50px 40px;
      margin-bottom: 70px;
      background: linear-gradient(180deg, #11131a 0%, #0b0c12 100%);
      border: 1px solid rgba(255,255,255,.06);
    }

    .mc-encomenda__kicker {
      font-size: 12px;
      letter-spacing: .25em;
      text-transform: uppercase;
      opacity: .6;
      margin-bottom: 14px;
    }

    .mc-encomenda__title {
      font-size: 34px;
      font-weight: 500;
      margin-bottom: 18px;
    }

    .mc-encomenda__subtitle {
      font-size: 16px;
      color: rgba(255,255,255,.75);
      max-width: 600px;
      line-height: 1.6;
      margin-bottom: 28px;
    }

    .mc-encomenda__cta-primary {
      padding: 14px 30px;
      border-radius: 999px;
      background: #ffffff;
      color: #0b0c12;
      font-weight: 600;
      font-size: 14px;
      text-decoration: none;
      display: inline-block;
      transition: all .2s ease;
      box-shadow: 0 10px 30px rgba(0,0,0,.25);
    }

    .mc-encomenda__cta-primary:hover {
      transform: translateY(-2px);
      opacity: .9;
    }

    .mc-encomenda__badge {
      margin-top: 20px;
      font-size: 12px;
      opacity: .6;
    }

    /* TRUST */

    .mc-encomenda__trust {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 20px;
      margin-bottom: 70px;
    }

    .mc-encomenda__trust-item {
      padding: 20px;
      border-radius: 16px;
      background: #141620;
      border: 1px solid rgba(255,255,255,.06);
      font-size: 14px;
      line-height: 1.5;
    }

    .mc-encomenda__trust-title {
      font-weight: 600;
      margin-bottom: 6px;
    }

    /* STEPS */

    .mc-encomenda__steps {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 24px;
      margin-bottom: 70px;
    }

    .mc-encomenda__step {
      padding: 24px;
      border-radius: 18px;
      background: #141620;
      border: 1px solid rgba(255,255,255,.06);
    }

    .mc-encomenda__step-index {
      font-size: 11px;
      text-transform: uppercase;
      opacity: .5;
      margin-bottom: 8px;
      letter-spacing: .2em;
    }

    .mc-encomenda__step-title {
      font-size: 16px;
      font-weight: 600;
      margin-bottom: 10px;
    }

    .mc-encomenda__step-text {
      font-size: 14px;
      opacity: .75;
      line-height: 1.6;
    }

    /* PRODUCT GRID */

    .mc-encomenda__products {
      margin-bottom: 80px;
    }

    .mc-encomenda__grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 24px;
    }

    .mc-encomenda__card {
      border-radius: 18px;
      overflow: hidden;
      background: #11131a;
      border: 1px solid rgba(255,255,255,.06);
      text-decoration: none;
      color: inherit;
      transition: all .2s ease;
    }

    .mc-encomenda__card:hover {
      transform: translateY(-4px);
      box-shadow: 0 20px 50px rgba(0,0,0,.6);
    }

    .mc-encomenda__media {
      aspect-ratio: 1 / 1;
      overflow: hidden;
      background: #0e0f14;
    }

    .mc-encomenda__media img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .mc-encomenda__body {
      padding: 16px;
    }

    .mc-encomenda__product-title {
      font-size: 14px;
      margin-bottom: 8px;
      min-height: 40px;
    }

    .mc-encomenda__price {
      font-size: 15px;
      font-weight: 600;
    }

    .mc-encomenda__prazo {
      font-size: 12px;
      opacity: .5;
      margin-top: 6px;
    }

    /* FAQ */

    .mc-encomenda__faq {
      padding: 40px;
      border-radius: 22px;
      background: #11131a;
      border: 1px solid rgba(255,255,255,.06);
    }

    .mc-encomenda__faq h2 {
      margin-bottom: 20px;
      font-weight: 500;
    }

    .mc-encomenda__faq details {
      padding: 14px 0;
      border-bottom: 1px solid rgba(255,255,255,.06);
    }

    .mc-encomenda__faq summary {
      cursor: pointer;
      font-weight: 500;
    }

    .mc-encomenda__faq p {
      margin-top: 10px;
      font-size: 14px;
      opacity: .75;
      line-height: 1.6;
    }

    /* RESPONSIVE */

    @media (max-width: 900px) {
      .mc-encomenda__trust,
      .mc-encomenda__steps,
      .mc-encomenda__grid {
        grid-template-columns: repeat(2, 1fr);
      }
    }

    @media (max-width: 600px) {
      .mc-encomenda__trust,
      .mc-encomenda__steps,
      .mc-encomenda__grid {
        grid-template-columns: 1fr;
      }

      .mc-encomenda__hero {
        padding: 30px 20px;
      }
    }
  </style>

  <div class="mc-encomenda__wrap">

    <!-- HERO -->
    <div class="mc-encomenda__hero">
      <div class="mc-encomenda__kicker">Coleção Sob Encomenda</div>
      <h1 class="mc-encomenda__title">Encomendas Especiais MyCharms</h1>
      <p class="mc-encomenda__subtitle">
        Peças fabricadas sob encomenda em prata S925, com acabamento premium e curadoria exclusiva. Produção e envio em até 50 dias úteis.
      </p>
      <a href="/collections/encomendas" class="mc-encomenda__cta-primary">
        Ver todas as peças
      </a>
      <div class="mc-encomenda__badge">
        Prazo médio real: 20 a 30 dias úteis.
      </div>
    </div>

  </div>
</section>
